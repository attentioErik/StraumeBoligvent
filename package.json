{
  "name": "straume-boligvent",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:studio": "npm run dev --prefix sanity",
    "build:web": "npm run build --prefix web",
    "install:all": "npm install --prefix web && npm install --prefix sanity"
  }
}
